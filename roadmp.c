//first agrs : string
//Second args : all value to replace into the string 

//ft_check -> '%' count	(not count double one) / compare it to args count

//ft_movesin_and_create: parcourir la chaine et faire appel a grab it a chaque %

//ft_check_grab and ft_grabit : conditionner la recuperation des flags (ni trop ni pas assez, surement des ifs ...)

//ft_check_args and type (

//faires des checker pour tous les flags / en fonction de la valeur de retour comportement different

Need to calcul size of value in args (for int for examle)
Convert to hexa function to

strange conversion to hexa when its negative number (ffff)

dontprint unprintable char

space one time (like "% I" same space as "%      i")
0sizetype -> si size > size number -> comble avec des 0
-sizetype -> si size > size number -> comble a droite avec des espaces
# -> 0x devant les hexas !
.sizeype afiche seulement les sizepremiers charchet (comble pas)
+type afiche un character +devant le nombre


//Take la string -> check for % 
	//if yes ->    

//return value : if fail -> return -1 else number of char displayed



//struct
	//%----
	//need to be modify or not bool
	//arg coorespondant
	//next node
	

//---------------------------------------------------------------//
	//index start
	//index len
	//buffindex
//substr best i guess to cut into buffer by index

		//while (*str)
			//if (str == %)
				//store dans la list chaine les character presdent avec substr puis new node to store this one 
				// (while str  after % is not (cppdiuxX%)) -> store in buffer
				//(then add to the list (the buffer, l'arg corresponding, next -> null) using sub
				//reset len to 0
				//buffindex = start (remettre lindex actuel (apres le (cpdix))

			//len++;
		//

/*-------------------------------------------------------------------------*/

	//when list is completed

	//some convertion function like hexa and address (maybe cast char *)	
	//Big function to handle all case of combo for bonus (. #+-0) priority etc

	//Create a new string from all element  -> BigSTRJOIN ! xD

/*----------------------------------------------------------------------------------*/

(to implement after passed)
{
	//if (% in the string ->check for same count of args and % count
		//if yes -> good 
		//else error 
		//else error
}

	//else display

	
